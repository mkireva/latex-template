\pagestyle{empty}

% Define the custom blue color
\definecolor{modernblue}{HTML}{3c8dbc}

% =================
% Modern Cover
% =================
\begin{center}
\begin{tikzpicture}[remember picture,overlay]

    % Clean blue background covering the entire page
    \fill[modernblue] (current page.south west) rectangle (current page.north east);

    % Subtle gradient overlay for depth
    \shade[top color=modernblue!90, bottom color=modernblue!100, opacity=0.3]
        (current page.south west) rectangle (current page.north east);

    % Author name (top of page, clean typography)
    \node[white, font=\large, anchor=north, text width=0.8\linewidth, align=center] 
        (author) at ([yshift=-60pt] current page.north) 
        {\textcolor{white}{\scshape\authorname}};

    % Book title (close to author, prominent)
    \node[white, font=\Huge, anchor=north, text width=0.9\linewidth, align=center, below=8pt of author.south] 
        (title) 
        {\textcolor{white}{\scshape\booktitle}};

    % Subtitle (optional, rendered only if defined)
    \ifx\subtitle\undefined\else
        \if\relax\detokenize\expandafter{\subtitle}\relax\else
            \node[white, font=\large, text width=0.8\linewidth, align=center, anchor=north, below=12pt of title.south] 
                {\textcolor{white!80}{\subtitle}};
        \fi
    \fi

    % Publisher logo (if exists)
    \node[anchor=south, above=60pt of current page.south] 
        {\includegraphics[width=0.08\textwidth]{frontmatter/logo-white.png}};

    % Publisher name at bottom
    \node[white, font=\large, anchor=south] 
        (publisher) at ([yshift=30pt] current page.south) 
        {\textcolor{white!90}{\scshape\publisher}};

    % Translator credit (optional, positioned elegantly)
    % \ifx\translatorname\undefined\else
    %     \if\relax\detokenize\expandafter{\translatorname}\relax\else
    %         \node[white, font=\normalsize, anchor=center, text width=0.7\linewidth, align=center] 
    %             (translatedBy) at ([yshift=-80pt] current page.center) 
    %             {\textcolor{white!70}{\itshape{Übersetzt und herausgegeben von}}};

    %         \node[white, font=\large, anchor=north, below=4pt of translatedBy.south] 
    %             (translatorAuthor) {\textcolor{white!90}{\itshape\translatorname}};       
    %     \fi
    % \fi        

\end{tikzpicture}
\end{center}

\cleardoublepage

% =================
% Title page
% =================
\begin{titlepage}
	\centering
	\newgeometry{top=1in,bottom=1in,right=0in,left=0in}
    \thispagestyle{empty}
	~	

    % Author name
	\vspace{24pt}
	{\scshape\large \authorname\par}

    % Book title
	\vspace{4pt}
	{\scshape\Huge \booktitle\par}

    % Subtitle (if defined)
    \ifx\subtitle\undefined\else
        \if\relax\detokenize\expandafter{\subtitle}\relax\else
            \vspace{6pt}
	        {\scshape\large \subtitle\par}
	        \vspace{\stretch{1.25}}
        \fi
    \fi

    % Translator (if defined)
    \ifx\translatorname\undefined\else
        \if\relax\detokenize\expandafter{\translatorname}\relax\else
            {\vspace{18pt}\itshape\large{Übersetzt und herausgegeben}\par}
	        \vspace{6pt}
            {\itshape\Large\translatorname\par}
        \fi
    \fi

    % Publisher (bottom of the page)
    \vspace{\stretch{6}}
	{\Huge\scshape\large\publisher\par}
\end{titlepage}
